{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="row justify-content-end">
        <div class="col-12 col-md-6 col-lg-3">
            <input type="submit" class="btn-submit w-100">
        </div>
    </div>
{{ form_end(form) }}