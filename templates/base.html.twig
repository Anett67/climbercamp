<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {{ encore_entry_link_tags('app') }}
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <header>
            <nav>
                <div class="logo">
                    <a href="{{ path('local-posts') }}"><img id="logo" src="{{ asset('build/images/icons/Logo.png') }}" alt=""></a> 
                </div>
                <div class="menu-block">
                    <ul class="navbar-nav">
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-md-none" href="" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <i class="fas fa-bars burger"></i>
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <a class="dropdown-item text-right" href="{{ path('local-posts') }}">Accueil</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item text-right" href="{{path('profil') }}">Mon Profile</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item text-right" href="{{path('friends') }}">Mes amis</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item text-right" href="{{ path('saved-clubs') }}">Salles d'escalade</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item text-right" href="{{ path('messages') }}">Messages</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item text-right" href="{{ path('saved-events', {'id' : app.user.id}) }}">Évènements enregistrés</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item text-right" href="{{ path('logout') }}">Déconnexion</a>
                            </div>
                        </li>
                    </ul>
                    <ul class="menu-list d-none d-md-flex">
                        <li class="menu-item"><a class="menu-link" href="{{ path('local-posts') }}" title="Accueil"><i class="fas fa-home d-none d-md-block"></i></a></li>
                        <li class="menu-item"><a class="menu-link" href="{{path('profil') }}" title="Mon Profil"><i class="fas fa-user d-none d-md-block"></i></a></li>
                        <li class="menu-item"><a class="menu-link" href="{{path('friends') }}" title="Mes amis"><i class="fas fa-users d-none d-md-block"></i></a></li>
                        <li class="menu-item"><a class="menu-link" href="{{path('saved-clubs') }}" title="Salles d'escalade préférées"><i class="fas fa-trophy"></i></a></li>
                        <li class="menu-item"><a class="menu-link" href="{{ path('messages') }}" title="Messages"><i class="far fa-envelope d-none d-md-block"></i></a></li>
                        <li class="menu-item"><a class="menu-link" href="{{ path('saved-events', {'id' : app.user.id}) }}" title="Évènements enregistrés"><i class="far d-none fa-calendar-alt d-md-block"></i></a></li>
                        <li class="menu-item"><a class="menu-link" href="{{ path('logout') }}" title="Déconnexion"><i class="fas fa-sign-out-alt d-none d-md-block"></i></li></a>
                    </ul>
                </div>
            </nav>
            {% block header %}
                <div class="header row justify-content-center align-items-center">
                    
                    <div class="card d-none d-lg-block col-2 text-center">
                        <div class="row justify-content-center">
                            <div class="img-cadre m-3">
                                <img class="card-image" src="{{ asset('upload/users/' ~ app.user.image) }}" alt="">
                            </div>
                        </div>
                        <div class="card-title"><a href="{{path('profil') }}">{{ app.user.firstName }} {{ app.user.lastName }}</a></div>    
                        <div class="card-text">
                            <div>{{ app.user.ville.nom }}</div>
                            <div>Niveau: <span class="level">{{ app.user.level.label }}</span></div>
                            <div>Salles d'escalade:
                                {% for salle in app.user.climbingClub %}
                                    <div><a class="club-link" href="{{ path('single-club', {'id': salle.id}) }}">{{ salle.nom }}</a></div>
                                {% endfor %}
                            </div>
                            <div class="row justify-content-center">
                                {% for type in app.user.climbingCategorie %}
                                    <div class="badge">{{ type.label }}</div>
                                {% endfor %}
                            </div> 
                        </div>

                        <div class="buttons">
                            <a href="{{ path('local-events') }}" class="btn event-button">Évènements à {{ app.user.ville.nom }}</a>
                            <a href="{{ path('local-users') }}" class="btn grimpeur-button">Grimpeurs à {{ app.user.ville.nom }}</a>
                            <a href="{{ path('local-clubs') }}" class="btn club-button">Salles d'escalade à {{ app.user.ville.nom }}</a>
                            <a href="{{ path('local-posts') }}" class="btn post-button">Publications de {{ app.user.ville.nom }}</a>
                        </div>
                    </div>
                    
                    <div class="image-block d-none d-md-block d-lg-none col-2">
                        {% block image %}
                            <div class="row justify-content-center">
                                <div class="img-cadre m-3">
                                    <img class="profilPicture" src="{{ asset('build/images/' ~ app.user.image) }}" alt="">
                                </div>
                            </div> 
                        {% endblock %}
                    </div>

                    <h1 class="col-8 text-center ml-lg-5 pl-lg-5">{% block pageTitle %}{% endblock %}</h1>
                
                </div>
            {% endblock %}
        </header>
        <div class="container">
        {% block body %}{% endblock %}
        </div>
        {% block javascripts %}{% endblock %}
        {{ encore_entry_script_tags('app') }}
    </body>
</html>
